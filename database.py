def get_user_mcids(discord_id):\n    """Fetches all MCIDs for a specific Discord user."""\n    # Assuming you have a database connection established\n    # Replace this with the actual database query to get MCIDs\n    mcids = []\n    query = "SELECT mcid FROM users WHERE discord_id = ?"\n    cursor.execute(query, (discord_id,))\n    results = cursor.fetchall()\n    for row in results:\n        mcids.append(row['mcid'])\n    return mcids